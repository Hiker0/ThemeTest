<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.allen.themetest"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
         >
       <activity
            android:name=".ListTest"
            android:configChanges="screenSize|keyboardHidden"
			android:theme="@style/ListTestTheme"
            
           >
             <intent-filter >
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
       </activity>  
       
         <activity
            android:name=".TabBarTest"
            android:configChanges="screenSize|keyboardHidden"
			 android:theme="@style/actionbartestTheme"
         > </activity> 
         <activity
            android:name=".PopupWindowTest"
            android:configChanges="screenSize|keyboardHidden"
			 android:theme="@style/DialogTestTheme"
         > </activity>         
         
    </application>

</manifest>
